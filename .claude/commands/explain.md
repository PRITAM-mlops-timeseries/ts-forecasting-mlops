Read the file or folder at path: $ARGUMENTS

Then explain it with:
1. **Purpose** — what this module/file is responsible for within the ts-forecasting-mlops project
2. **Phase** — which project phase introduced it and why it was needed at that point
3. **need** - Overall need of the file , Intent of making this.
4. **Report** - Create i 2-3 page .html guide in Explanation folder in root.

**Following points if aplicable **
5. **Key abstractions** — the main classes, ABCs, or patterns in play (e.g. BaseForecaster, Repository pattern, Strategy pattern) and how they connect to the SOLID principles documented in CLAUDE.md if any
6. **Interfaces** — what this module exposes to the rest of the codebase, and what it depends on
7. **Gotchas** — any non-obvious constraints, design decisions, or invariants a developer must know before modifying this code

Keep the explanation grounded in the actual code — no generic descriptions.
